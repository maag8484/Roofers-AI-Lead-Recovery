// Single source of truth for the plan label. The product is single-plan; there
// is no plan/price column on subscriptions. Referenced by the customers list
// and the customer detail page — never inline the string.
export const PLAN_LABEL = "Roof AI · $299/mo";
